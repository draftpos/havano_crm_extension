<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View Extension -->
    <record id="crm_lead_view_form_inherit_custom" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <!-- Place the 3-column details directly in the main form area before the tabs -->
            <!-- Hide the standard Odoo 2-column top group to prevent duplicate layout -->
            <xpath expr="//sheet/group" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!-- Place our unified 3-column details directly in the main form area before the tabs -->
            <xpath expr="//notebook" position="before">
                <div class="row mt-3 mb-3">
                    <!-- Column 1: Contact & Basic Info -->
                    <div class="col-md-4">
                        <group>
                            <field name="contact_name" string="First Name"/>
                            <field name="partner_name" string="Organization/Store Name"/>
                            <field name="email_from" string="Email"/>
                            <field name="phone" string="Phone"/>
                            <field name="website" string="Website"/>
                            <field name="source_id" string="Source"/>
                            <field name="product_id" string="Product Interest"/>
                        </group>
                    </div>
                    
                    <!-- Column 2: Deal & Sales Info -->
                    <div class="col-md-4">
                        <group>
                            <field name="user_id" string="Salesperson"/>
                            <field name="team_id" string="Sales Team"/>
                            <field name="priority" widget="priority"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}"/>
                            <field name="custom_deal_size" string="Deal Size $"/>
                            <field name="custom_quote" string="Quote"/>
                            <field name="custom_quote_date" string="Quote Date"/>
                            <field name="custom_proposal_status" string="Proposal"/>
                            <field name="custom_proposal_date" string="Proposal Date"/>
                        </group>
                    </div>

                    <!-- Column 3: Address & Additional Info -->
                    <div class="col-md-4">
                        <group>
                            <field name="city" string="City/Town"/>
                            <field name="street" string="Full Address"/>
                            <field name="custom_technician" string="Technician"/>
                            <field name="custom_type_of_business" string="Type of Business"/>
                            <field name="custom_demo_done" string="Demo Done"/>
                            <field name="custom_demo_type" string="Demo Done Online/Onsite" invisible="custom_demo_done == False"/>
                        </group>
                    </div>
                </div>
            </xpath>

            <!-- Hide the extra information tab to keep the UI clean and prevent repetition -->
            <xpath expr="//page[@name='extra']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!-- Show original todo_ids under Activities tab with column toggling enabled -->
            <xpath expr="//notebook" position="inside">
                <page string="Activities &amp; To-Dos" name="imported_todos">
                    <field name="todo_ids">
                        <list string="To-Dos" editable="bottom">
                            <field name="name" string="Subject"/>
                            <field name="status" optional="show"/>
                            <field name="date" optional="show"/>
                            <field name="allocated_to" optional="show"/>
                            <field name="legacy_create_date" string="Original Creation Date" optional="show"/>
                        </list>
                    </field>
                </page>
            </xpath>

        </field>
    </record>

    <!-- Tree / List View Extension to show Last To-Do Note & Product Interest -->
    <record id="crm_lead_view_tree_inherit_custom" model="ir.ui.view">
        <field name="name">crm.lead.tree.inherit.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_leads"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="last_todo_note" string="Last To-Do / Note" optional="show"/>
                <field name="product_id" string="Product Interest" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Opportunities Tree View Extension as well -->
    <record id="crm_opportunity_view_tree_inherit_custom" model="ir.ui.view">
        <field name="name">crm.opportunity.tree.inherit.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_oppor"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="last_todo_note" string="Last To-Do / Note" optional="show"/>
                <field name="product_id" string="Product Interest" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
